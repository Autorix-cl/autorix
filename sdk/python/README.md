# Autorix Python SDK

Typed client for Autorix public runtime APIs. Install extras when using a web framework:

```bash
pip install 'autorix[fastapi]'
```

The SDK exposes public runtime APIs only. OAuth client secrets must only be used in backend processes.
